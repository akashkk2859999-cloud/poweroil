{
  "name": "ugc-masterchef-campaign",
  "version": "1.0.0",
  "description": "PowerOil x MasterChef Nigeria UGC Campaign Microsite",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "install:all": "npm install --workspaces && npm install",
    "dev": "concurrently \"npm run dev --workspace=backend\" \"npm run dev --workspace=frontend\"",
    "setup": "npm run install:all && npm run db:migrate --workspace=backend && npm run db:seed --workspace=backend",
    "build": "npm run build --workspace=frontend && npm run build --workspace=backend",
    "start": "npm run start --workspace=backend"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
